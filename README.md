This is a very bloated project but it's a necessary sacrifice to run it in my college computers

to debug, tap F5 or run the debug task in .vscode/tasks.json
to build in release mode, run the release task in .vscode/tasks.json

upping the number at line 56 in main.cpp will make the game harder by making the opponent move faster
upping the max variable at the top sets the ball's max speed